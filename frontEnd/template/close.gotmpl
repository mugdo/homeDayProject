{{ define "close" }}

{{/* html tag open
    head tag open
        head tag content here
    head tag close

    body tag open
        main div open
            header content here
            navbar content here
            main page content here */}}
            
            {{ template "footer" . }}
        </div>
        <script src="../assets/scripts/main.js"></script>
        {{ if eq .pageTitle "Verdict" }}
            {{ template "verdictScript" . }}
        {{ else if eq .pageTitle "Submission" }}
            {{ template "submissionScript" . }}
        {{ else if eq .pageTitle "Registration" }}
            {{ template "registerScript" . }}
        {{ else if and (eq .pageTitle "Login") (or (eq .LastPage "RegistrationDone") (eq .LastPage "tokenInvalid") (eq .LastPage "tokenAlreadyVerified") (eq .LastPage "tokenExpired") (eq .LastPage "tokenVerifiedNow") (eq .LastPage "tokenRequest")) }}
            {{ template "popUpLoginScript" . }}
            {{ template "loginScript" . }}
        {{ else if eq .pageTitle "Login" }}
            {{ template "loginScript" . }}
        {{ else if and (eq .pageTitle "Homepage") (or (eq .LastPage "RegistrationDone") (eq .LastPage "tokenInvalid") (eq .LastPage "tokenAlreadyVerified") (eq .LastPage "tokenExpired") (eq .LastPage "tokenVerifiedNow") (eq .LastPage "tokenRequest")) }}
            {{ template "popUpLoginScript" . }}
        {{ end }}
    </body>
</html>

{{ end }}