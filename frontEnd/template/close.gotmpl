{{ define "close" }}

{{/* html tag open
    head tag open
        head tag content here
    head tag close

    body tag open
        main div open
            header content here
            navbar content here
            main page content here */}}
            
            {{ template "footer" . }}
        </div>
        <script src="../assests/scripts/main.js"></script>
        {{ if eq .pageTitle "Verdict" }}
            {{ template "verdictScript" . }}
        {{ else if eq .pageTitle "Submission" }}
            {{ template "submissionScript" . }}
        {{ else if eq .pageTitle "Registration" }}
            {{ template "registerScript" . }}
        {{ else if and (eq .pageTitle "Login") (or (or (eq .LastPage "tokenInvalid") (eq .LastPage "tokenAlreadyVerified")) (or (eq .LastPage "tokenExpired") (eq .LastPage "tokenVerifiedNow")) ) }}
            {{ template "popUpLoginScript" . }}
        {{ else if and (eq .pageTitle "Homepage") (or (or (eq .LastPage "tokenInvalid") (eq .LastPage "tokenAlreadyVerified")) (or (eq .LastPage "tokenExpired") (eq .LastPage "tokenVerifiedNow")) ) }}
            {{ template "popUpLoginScript" . }}
        {{ end }}
    </body>
</html>

{{ end }}